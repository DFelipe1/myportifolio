import { styled } from "../../stitches.config";

export const Layout = styled('div', {
    width: "800px",
    minHeight: '100vh',
    margin: '0 auto',
    padding: '$6 0',

    display: 'flex',
    flexDirection: 'column',
    gap: '$10'
})